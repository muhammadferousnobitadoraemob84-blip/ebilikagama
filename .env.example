# Server-only secrets (NEVER NEXT_PUBLIC_*, never committed)
FIREBASE_SERVICE_ACCOUNT={"project_id":"ebilikagama-broadcast", ...}
FIREBASE_STORAGE_BUCKET=ebilikagama-broadcast.firebasestorage.app
FIREBASE_PROJECT_ID=ebilikagama-broadcast
JWT_SECRET=change-me

# Optional public web-config overrides (fallbacks baked into src/lib/firebase.ts)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=ebilikagama-broadcast
